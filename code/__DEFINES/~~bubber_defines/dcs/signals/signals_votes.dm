// Sent when a vote starts on the SSvote subsystem
#define COMSIG_VOTE_STARTED "vote_start"
// Sent when a vote ends on the SSvote subsystem
#define COMSIG_VOTE_ENDED "vote_end"

// Sent when you select a choice on the HUD element of the mapvote
#define COMSIG_VOTE_CHOICE_SELECTED "vote_select"
