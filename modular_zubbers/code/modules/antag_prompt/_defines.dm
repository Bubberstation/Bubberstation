/// Shared "Never For This Round" category, so opting out of one antag prompt opts you out of all of them.
#define ANTAG_PROMPT_IGNORE_CATEGORY "Antagonist Prompt"

/// Log category for antag prompts, writes to antag-prompt.log
#define LOG_CATEGORY_ANTAG_PROMPT "antag-prompt"
