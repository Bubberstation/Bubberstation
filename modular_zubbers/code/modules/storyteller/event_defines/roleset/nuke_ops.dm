/* /datum/round_event_control/antagonist/nuke_ops
	name = "Nuclear Operatives"
	roundstart = TRUE

	antag_flag = ROLE_NUCLEAR_OPERATIVE
	antag_datum = /datum/antagonist/nukeop
	weight = 1
	tags = list(TAG_CREW_ANTAG)

 */

 // Don't think it will work so simply.
